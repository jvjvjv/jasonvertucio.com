## 1. Baseline and dependency bump

- [x] 1.1 Run `vendor/bin/phpunit` on `develop` before touching anything and record the passing count, so later steps have a clean baseline to compare against.
- [x] 1.2 Read the removed package's v0.10.0 source for the classes this change replaces, via `git show v0.10.0:<path>` in `/home/jasonv/Code/@jvjvjv/code-talker` — at minimum `Http/Controllers/ChatBotController.php`, `Services/ChatBot/ConversationSessionStore.php`, `Http/Controllers/Admin/AiSystemPromptController.php`, `Http/Controllers/Admin/AiMemoryController.php`. Keep these open for reference through section 3.
- [x] 1.3 Bump `"jvjvjv/code-talker": "^0"` to `"^0.11"` in `composer.json` and run `composer update jvjvjv/code-talker`. Confirm `composer.lock` now records `v0.11.0`.
- [x] 1.4 Run `php artisan migrate` and confirm `2026_08_16_000001_add_message_structure_to_ai_conversation_messages_table` applies cleanly against the dev database. Also run `DB_DATABASE=wink php artisan migrate` against the test database — `phpunit.xml` points tests at `wink`, a separate database migrations don't reach automatically. Missing this surfaced as a "column not found: tool_calls" turn failure in `AiChatBotConversationServiceTest`.
- [x] 1.5 Run `composer install` / `php artisan config:clear` and confirm the app still boots (`php artisan route:list` should now show missing-class errors for the routes this change fixes — expected at this point, not a regression).
- [x] 1.6 (Discovered during implementation, not anticipated in design.md) `app/Http/Controllers/Admin/AiToolsController.php` also extends a removed package controller (`Jvjvjv\CodeTalker\Http\Controllers\Admin\AiToolsController`) — missed by the original file audit. Rewrite it as a plain host controller; its removed parent was a single `index()` method rendering `ai/Index` with no props, trivial to reproduce in full.
- [x] 1.7 (Discovered during implementation) code-talker 0.11.0's `CodeTalkerServiceProvider::register()` binds `Laravel\Ai\Contracts\ConversationStore` to `CodeTalkerConversationStore`, but Laravel registers auto-discovered package providers alphabetically — `laravel/ai`'s own provider registers after `jvjvjv/code-talker`'s and re-binds the same interface back to its default `DatabaseConversationStore`, silently clobbering it. Symptom: `agent_conversation_messages` table-not-found errors (the framework default's table) instead of the package's own `ai_conversation_messages`. Fix: rebind `ConversationStore::class` to `CodeTalkerConversationStore::class` in `AppServiceProvider::register()`, which registers last (host `config('app.providers')` entries load after the package manifest).

## 2. Clear dead container bindings and payload services

- [x] 2.1 In `app/Providers/AppServiceProvider.php`, remove the five bindings keyed on now-removed package classes (`BaseChatBotIndexPayload`, `BaseChatBotPagePayload`, `BaseChatBotStatusResolver`, `BaseStoreAiChatBotRequest`, `BaseUpdateAiChatBotRequest`) and their `use` imports. Keep `Route::model('aiChatBot', ...)`, `addToolDirectory()`, and `registerToolParameterResolver()` untouched.
- [x] 2.2 Delete `app/Services/ChatBot/HostChatBotPagePayload.php`, `RoleFilteredChatBotIndexPayload.php`, and `RoleFilteredChatBotStatusResolver.php` — their base classes no longer exist. Note what role-filtering logic each contained before deleting; it moves into the new host controller in section 3.

## 3. Rebuild the public chat-bot controller

- [x] 3.1 Create `app/Services/ChatBot/ChatBotSessionStore.php` (or similar), reproducing `ConversationSessionStore`'s v0.10.0 contract: `currentConversation(Request, AiChatBot): ?AiConversation`, `remember(Request, AiChatBot, AiConversation): void`, `switchTo(Request, AiChatBot, string $publicId): bool`, `startNewChat(Request, AiChatBot): void`, `forgetLegacyCookies(Request): void`. State lives in the session, keyed per bot; mirror only the current conversation id into a single `ai_chat_bot_current` cookie (matching the removed class's cookie name and ~180-day expiry so any live cookies from before the upgrade keep working). History list capped at 25 entries per bot.
- [x] 3.2 Rewrite `app/Http/Controllers/ChatBotController.php` as a full controller (no longer extending anything from the package), injecting `AiChatBotConversationService`, `AiModelReadinessService`, `ChatBotSessionStore`, and `SseFrameEncoder`. Implement `index()`, `statuses()`, `show()`, `showByHash()`, `status()`, `warmup()`, `message()`, `switch()`, `reset()`, `newChat()`, matching the removed package controller's method bodies (from task 1.2) but sourcing role-filtered bot lists/statuses from the logic salvaged in task 2.2 instead of container-swapped payload classes.
- [x] 3.3 In `message()`, stream `continueConversation()` through `SseFrameEncoder::encode()` inside a `StreamedResponse`, and set the `X-Chat-Hash` response header from `$conversation->generateChatHash()` before streaming starts (frontend depends on this header per the 0.10.0 contract work).
- [x] 3.4 Build the Inertia page-prop payloads for `show()`/`showByHash()` directly from `AiConversation`/`AiChatBot` models plus the package's `ChatBotPresenter::transcript()`, matching the `ChatBotPageProps` shape in `resources/js/types/code-talker.d.ts`, and adding the host's `bot.allowed_roles` and `previousHref` per the `host-chat-bot-presentation` spec.
- [x] 3.5 Confirm the identity-form gating logic (`showIdentityForm`) and the `includeChatHash` flag on `showByHash()` are preserved exactly as in the removed controller.
- [x] 3.6 Run `php artisan route:list --path=chat` and confirm every route in `routes/codetalker-chatbots.php` resolves without error.

## 4. Rebuild the admin AI controllers

- [x] 4.1 In `app/Http/Requests/Admin/StoreAiChatBotRequest.php` and `UpdateAiChatBotRequest.php`, replace `extends BaseStoreAiChatBotRequest` / `extends BaseUpdateAiChatBotRequest` with plain `FormRequest`, sourcing `rules()` from `Jvjvjv\CodeTalker\Services\Management\AiChatBotManager::createRules()` / `updateRules()`, merged with the host's existing `allowed_roles` rule.
- [x] 4.2 In `app/Http/Controllers/Admin/AiChatBotController.php`, remove `extends BaseAiChatBotController`, give it its own base (`Controller`), and add explicit `destroy()` and `mcpTools()` bodies calling `AiChatBotManager::delete()` and the manager's tool-listing method — every other method is already overridden per the file's own doc comment.
- [x] 4.3 In `app/Http/Requests/StoreAiSystemRequest.php` / `UpdateAiSystemRequest.php`, source `rules()` from `AiSystemManager::createRules()` / `updateRules()` instead of whatever the removed controller previously validated inline.
- [x] 4.4 Confirm `app/Http/Controllers/Admin/AiSystemController.php` (already host-owned) needs no structural changes beyond its form requests — verify it doesn't import any removed package controller class.
- [x] 4.5 Create `app/Http/Requests/Admin/StoreAiSystemPromptRequest.php` and `UpdateAiSystemPromptRequest.php` sourcing rules from `AiSystemPromptManager::rules()`.
- [x] 4.6 Create `app/Http/Controllers/Admin/AiSystemPromptController.php`, reproducing the removed package controller's five actions (`index`, `create`, `store`, `edit`, `update`, `destroy`, `apiUpdate`) against `AiSystemPromptManager`, rendering the existing `admin/pages/ai/system-prompts/*` Inertia pages, following `AiChatBotController`'s pattern (`ProvidesAdminNavigation`, redirect-with-flash).
- [x] 4.7 Create `app/Http/Requests/Admin/StoreAiFeatureMemoryRequest.php` and `UpdateAiFeatureMemoryRequest.php` sourcing rules from `AiMemoryManager::createRules()` / `updateRules()`.
- [x] 4.8 Create `app/Http/Controllers/Admin/AiMemoryController.php`, reproducing the removed package controller's actions (`index` with feature/category/status filters, `create`, `store`, `edit`, `update`, `destroy`, `rebuild`) against `AiMemoryManager`, rendering the existing `admin/pages/ai/memories/*` Inertia pages.
- [x] 4.9 Update `routes/codetalker-admin.php` to import the new host `AiSystemPromptController`/`AiMemoryController` instead of the package classes, and update `routes/api-web.php`'s `apiUpdate` route the same way.
- [x] 4.10 Run `php artisan route:list --path=admin/ai` and confirm every route resolves without error.

## 5. Own route loading

- [x] 5.1 Confirm whether `routes/codetalker-chatbots.php` and `routes/codetalker-admin.php` are currently loaded via the package's conditional `booted()` override mechanism or already `require`d from `routes/web.php`. If the former, add unconditional `require` calls for both files (plus `routes/api-web.php` if not already required) into `routes/web.php`, matching how the rest of the app's route files are loaded.
- [x] 5.2 Run `php artisan route:list` and diff the full route table against a pre-upgrade capture (from before task 1.3) to confirm no route was dropped or duplicated.

## 6. Tool policy and collision checks

- [x] 6.1 Grep `app/Services/Mcp/Tools/` for any tool already named `http-request` (the changelog's documented collision risk since host tool directories are discovered after package ones). If one exists, rename it or the package tool will be shadowed silently.
- [x] 6.2 Audit every `fetch-web-page` call site (chat-bot tool configs, `app/Services/Mcp/Tools/TargetedResume/*` if applicable) for use against loopback/private-network hosts (local Jellyfin, LM Studio at `localhost:1234`, etc.). Add `request_policy.allow_private_hosts` to any bot/tool config that legitimately needs it; leave everything else on the new public-hosts-only default.
- [x] 6.3 Confirm any turn-running path outside an HTTP request (queued jobs, Artisan commands using `AiChatBotConversationService`) supplies `usingCancellationCheck()` explicitly, since `connection_aborted()` reports 0 outside a request context. Check `app/Console/Commands/` and any queued job that drives a conversation turn.

## 7. Verification

- [x] 7.1 Run `vendor/bin/phpunit` and compare against the task 1.1 baseline — investigate any newly failing test before proceeding.
- [ ] 7.2 Manually exercise: open `/chats`, confirm role-filtered listing for a guest and an authenticated user; open a bot, send two messages with no hash in the URL, confirm both land in the same conversation; reload via the hash URL; use `switch()` to jump to a different prior conversation for the same bot; confirm `reset()`/new-chat starts a fresh conversation while the old one remains reachable via switch.
- [ ] 7.3 Manually exercise one full CRUD cycle each for AI Systems, System Prompts, Chat Bots, and Memories under `/admin/ai/*`, including the Memories "rebuild" action and the System Prompts inline-edit modal (`apiUpdate`).
- [ ] 7.4 Re-run the outstanding manual check from `openspec/changes/adopt-code-talker-frontend-contract` task 5.7 (targeted-resume builder: tool activity indicator, panel reload after save) since it exercises conversation streaming this change touches, even though `TargetedResumeService` itself is out of scope.
- [x] 7.5 Confirm no references to the removed package classes remain: `grep -rn "Jvjvjv\\\\CodeTalker\\\\Http\\\\Controllers\\\\ChatBotController\\|Jvjvjv\\\\CodeTalker\\\\Http\\\\Controllers\\\\Admin\\\\Ai\\(ChatBot\\|SystemPrompt\\|Memory\\)Controller\\|ChatBotIndexPayload\\|ChatBotPagePayload\\|ChatBotStatusResolver\\|ChatBotAccessGuard\\|ChatBotRouteUrls\\|ConversationHistoryPresenter\\|ConversationSessionStore\\|ChatStreamResponse" app/ routes/` returns nothing.
- [x] 7.6 Run `npm run build` to confirm the frontend still compiles against the unchanged Inertia prop shapes (no frontend files should need edits from this change; a build failure signals a prop-shape mismatch to investigate).
